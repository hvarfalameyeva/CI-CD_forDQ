pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/your-username/your-repo.git'
            }
        }

        stage('Run tests') {
            steps {
                sh 'python3 -m DQ_module4_halaveika.py'
            }
        }
    }
}
